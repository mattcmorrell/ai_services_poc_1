import { InterviewChat } from "@/components/interview-chat";

export const metadata = {
  title: "Customer Interview — BambooHR AI",
};

export default function InterviewPage() {
  return <InterviewChat />;
}
